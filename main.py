"""
Rune AI — Entry Point

This is the single entry point for the application. It imports the app module
and runs the Qt event loop, which keeps the window alive and responsive.
"""

from app import run


if __name__ == "__main__":
    run()
